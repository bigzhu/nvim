-- Local overrides (not committed). Copy to lua/config/local.lua and edit.
-- Examples: machine-specific paths, secret keys, extra keymaps.

local M = {}

-- Example: provide workspace paths via environment variables in your shell rc
--   export OBSIDIAN_DIR="$HOME/Sync/home/cheese"
--   export OBSIDIAN_DEV_DIR="$HOME/Sync/Projects/obsidian.nvim"

-- Example: extra keymaps
-- vim.keymap.set("n", "<leader>ox", ":echo 'local override'\n<CR>", { desc = "Local test" })

return M

